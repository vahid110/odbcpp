@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find required dependencies
find_dependency(OpenSSL REQUIRED)

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/odbcppTargets.cmake")

check_required_components(odbcpp)